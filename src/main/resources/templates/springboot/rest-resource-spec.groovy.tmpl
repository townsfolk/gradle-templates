package ${servicePackage}.resources

import ${servicePackage}.ComponentTest
import spock.lang.Specification

import static com.blackbaud.ARandom.aRandom

@ComponentTest
class ${resourceName}Spec extends Specification {

}