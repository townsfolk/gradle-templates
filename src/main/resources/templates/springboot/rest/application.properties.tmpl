spring.jersey.type=servlet
server.servlet-path=/admin
server.port=-1

# disable security for verbose /health output
endpoint.health.sensitive=false
management.security.enabled=false

# disable basic auth, we use a custom auth filter to parse bearer tokens
security.basic.enabled=false

swagger.resource.package=${resourcePackageName}
