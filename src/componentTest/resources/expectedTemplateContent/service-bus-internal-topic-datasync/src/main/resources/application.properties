server.port=-1

# disable basic auth, we use a custom auth filter to parse bearer tokens
security.basic.enabled=false

swagger.resource.package=com.blackbaud.service.resources
bbauth.enabled=true
long.token.enabled=false
servicebus.datasync.session_enabled=false
servicebus.datasync.producer_connection_url=${APPSETTING_ServiceBus__datasync__Send}
servicebus.datasync.consumer_connection_url=${APPSETTING_ServiceBus__datasync__Listen}
